@model IEnumerable<SearchResultProductDTO.ProductNARADTO>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(item => item.NombreNARA)
            </th>
            <th>
                @Html.DisplayNameFor(item => item.DescripcionNARA)
            </th>
            <th>
                @Html.DisplayNameFor(item => item.Precio)
            </th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NombreNARA)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DescripcionNARA)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Precio   )
                </td>
                <td>
                    @Html.ActionLink("Modificar", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Ver", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Eliminar", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
